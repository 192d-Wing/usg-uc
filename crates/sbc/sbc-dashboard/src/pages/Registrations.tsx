import { PlaceholderPage } from '../components/PlaceholderPage';

export function Registrations() {
  return (
    <PlaceholderPage
      title="Registrations"
      description="Live SIP registration bindings. Will replace the Angular RegistrationsComponent with a Cloudscape Table backed by /api/v1/registrations."
    />
  );
}
