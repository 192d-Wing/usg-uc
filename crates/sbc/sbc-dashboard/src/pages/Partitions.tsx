import { PlaceholderPage } from '../components/PlaceholderPage';

export function Partitions() {
  return (
    <PlaceholderPage
      title="Partitions"
      description="CUCM-style call routing partitions. Backed by /api/v1/partitions."
    />
  );
}
