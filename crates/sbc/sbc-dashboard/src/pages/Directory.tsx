import { PlaceholderPage } from '../components/PlaceholderPage';

export function Directory() {
  return (
    <PlaceholderPage
      title="Directory Numbers"
      description="Provisioned extensions and their owning users. Backed by /api/v1/directory."
    />
  );
}
