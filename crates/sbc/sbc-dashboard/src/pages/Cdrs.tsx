import { PlaceholderPage } from '../components/PlaceholderPage';

export function Cdrs() {
  return (
    <PlaceholderPage
      title="CDR Records"
      description="Call detail records from /api/v1/cdrs. Will paginate with Cloudscape Table + DateRangePicker."
    />
  );
}
