import { PlaceholderPage } from '../components/PlaceholderPage';

export function RouteLists() {
  return (
    <PlaceholderPage
      title="Route Lists"
      description="Ordered route group lists used by route patterns to direct calls."
    />
  );
}
