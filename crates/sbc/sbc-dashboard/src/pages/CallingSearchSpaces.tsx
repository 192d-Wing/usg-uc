import { PlaceholderPage } from '../components/PlaceholderPage';

export function CallingSearchSpaces() {
  return (
    <PlaceholderPage
      title="Calling Search Spaces"
      description="Ordered partition lists that determine which numbers a caller can reach."
    />
  );
}
