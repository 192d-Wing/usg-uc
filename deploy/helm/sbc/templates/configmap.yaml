apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sbc.fullname" . }}-config
  labels:
    {{- include "sbc.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- include "sbc.config" . | nindent 4 }}
